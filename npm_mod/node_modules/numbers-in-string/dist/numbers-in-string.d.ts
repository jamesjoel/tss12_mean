declare namespace numbersInString {
  // export default function numbers(str: string, filter: Function): number[];
}

/**
 * Extracts numbers from a string.
 *
 * @param {string} str The string to extract numbers from.
 * @param {function} filter An optional function filter function.
 * @returns {number[]} Array of extracted numbers.
 */
declare function numbers(str: string, filter?: Function): number[];

export = numbers;
